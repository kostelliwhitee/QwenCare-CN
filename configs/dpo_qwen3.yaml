stage: dpo
base_model_name_or_path: runs/sft_qwen3_lora
preference_data_path: data/processed/dpo_pairs.json
output_dir: runs/dpo_qwen3
cuda_visible_devices: "5,6"

training:
  per_device_train_batch_size: 1
  gradient_accumulation_steps: 16
  num_train_epochs: 1
  learning_rate: 5.0e-6
  beta: 0.1
  bf16: true
  logging_steps: 10
